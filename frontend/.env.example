# Network the frontend talks to. Use "preprod" for the Midnight Preprod testnet.
VITE_NETWORK_ID=preprod

# Address of an already-deployed VeilVote contract instance (one proposal).
# Leave blank to use the in-app "Deploy a new proposal" flow instead.
VITE_CONTRACT_ADDRESS=
