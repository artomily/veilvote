{
  "framework": null,
  "buildCommand": "npm install && npm run build --workspace=frontend",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo 'install runs as part of buildCommand'"
}
